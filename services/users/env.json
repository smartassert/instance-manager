{
  "IS_READY": "1",
  "JWT_SECRET_KEY_BASE64": "{{ secrets.USERS_JWT_SECRET_KEY_BASE64 }}",
  "JWT_PUBLIC_KEY_BASE64": "{{ secrets.USERS_JWT_PUBLIC_KEY_BASE64 }}",
  "JWT_PASSPHRASE": "{{ secrets.USERS_JWT_PASSPHRASE }}"
}
